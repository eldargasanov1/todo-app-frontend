import { connect } from 'react-redux';
import { TaskEntity } from './TaskEntity';

const mapStateToProps = state => ({});
const mapDispatchToProps = dispatch => ({});

export default connect(mapStateToProps, mapDispatchToProps)(TaskEntity);
